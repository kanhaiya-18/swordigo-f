export default {
  content: [
    "./index.html",
    "./src/**/*.{js,jsx,ts,tsx}"
  ],
  theme: {
    extend: {
      // put custom colors here. don't use `theme: { colors: { ... } }` unless you want to remove the default colors like gray-900
    }
  },
  plugins: [],
}